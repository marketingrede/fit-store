{% extends 'layouts/admin.twig' %}

{% block title %}{{ employee ? 'Editar' : 'Novo' }} Elegível | Admin {{ app_name }}{% endblock %}

{% block admin_content %}
  <div class="admin-page-header">
    <div>
      <h1 class="admin-page-header__title">{{ employee ? 'Editar Elegível' : 'Novo Elegível' }}</h1>
      <p class="admin-page-header__subtitle">{{ employee ? 'Atualize os dados do colaborador elegível' : 'Adicione um novo colaborador à whitelist' }}</p>
    </div>
    <div class="admin-page-header__actions">
      <a href="/admin/colaboradores/elegiveis" class="btn-admin secondary"><i data-lucide="arrow-left"></i> Voltar</a>
    </div>
  </div>

  {% if error %}
    <div class="admin-alert admin-alert--error">{{ error }}</div>
  {% endif %}

  <div class="admin-form-card">
    <form method="post" action="{{ employee ? '/admin/colaboradores/elegiveis/' ~ employee.id : '/admin/colaboradores/elegiveis' }}">
      <input type="hidden" name="_csrf" value="{{ csrf_token() }}">

      <div class="form-grid">
        <div class="field">
          <label for="employee_id">Matrícula *</label>
          <input type="text" id="employee_id" name="employee_id" value="{{ employee.employee_id ?? '' }}" required {{ employee ? 'readonly style="background:var(--brand-surface-alt);cursor:not-allowed"' : '' }}>
        </div>
        <div class="field">
          <label for="full_name">Nome completo *</label>
          <input type="text" id="full_name" name="full_name" value="{{ employee.full_name ?? '' }}" required>
        </div>
        <div class="field">
          <label for="email">E-mail</label>
          <input type="email" id="email" name="email" value="{{ employee.email ?? '' }}">
        </div>
        <div class="field">
          <label for="department">Departamento</label>
          <input type="text" id="department" name="department" value="{{ employee.department ?? '' }}">
        </div>
        <div class="field">
          <label for="initial_balance_fitc">Saldo inicial (FITC)</label>
          <input type="number" id="initial_balance_fitc" name="initial_balance_fitc" value="{{ employee.initial_balance_fitc ?? 0 }}" min="0">
        </div>
        {% if employee %}
          <div class="field">
            <label for="status">Status</label>
            <select id="status" name="status">
              <option value="active" {{ employee.status == 'active' ? 'selected' : '' }}>Ativo</option>
              <option value="suspended" {{ employee.status == 'suspended' ? 'selected' : '' }}>Suspenso</option>
              <option value="revoked" {{ employee.status == 'revoked' ? 'selected' : '' }}>Revogado</option>
            </select>
          </div>
        {% endif %}
        <div class="field field--full">
          <label for="notes">Observações</label>
          <textarea id="notes" name="notes" rows="3">{{ employee.notes ?? '' }}</textarea>
        </div>
      </div>

      <div class="form-actions">
        <button type="submit" class="btn-admin"><i data-lucide="save"></i> {{ employee ? 'Salvar alterações' : 'Criar elegível' }}</button>
        <a href="/admin/colaboradores/elegiveis" class="btn-admin secondary">Cancelar</a>
      </div>
    </form>
  </div>
{% endblock %}