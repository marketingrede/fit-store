services:
  app:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./data:/var/www/html/data
      - ./public/uploads:/var/www/html/public/uploads
    env_file:
      - .env
    restart: unless-stopped
