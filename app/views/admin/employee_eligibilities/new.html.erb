<% content_for :title, (@eligibility.persisted? ? "Editar elegível" : "Novo elegível") %>

<%= render "admin/shared/page_header", title: (@eligibility.persisted? ? "Editar elegível" : "Novo elegível") %>

<%= render "form" %>
