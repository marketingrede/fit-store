# Arquivos estáticos originais (pré-migração Slim)

Estes arquivos foram substituídos pela aplicação PHP em `public/index.php`.

- `index.html` → `GET /`
- `produto.html` → `GET /produto/{id}`
- `Script.js` → banco SQLite + seed
- `send_email.php` → `POST /api/troca-fitcoin`
- `styles.css` → `resources/css/store.css`
